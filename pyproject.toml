[tool.poetry]
name = "pyhouse"
version = "0.1.0"
description = ""
authors = ["Anarco"]
license = "GPL-2.0+"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
clickhouse-connect = "^0.7.19"
sqlglot = {extras = ["rs"], version = "^25.22.0"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
